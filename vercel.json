{
  "installCommand": "yum install -y hostname && npm i -g pnpm && pnpm install",
  "buildCommand": "pnpm nx build pochoui-app --prod && pwd && ls -la && ls -la dist && ls -la dist/apps",
  "outputDirectory": "dist/apps/pochoui-app",
  "framework": "vite"
}
